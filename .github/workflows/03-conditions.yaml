name: conditions
on: [push] #triggers
jobs: 
  hello: 
    runs-on: ubuntu-latest # linux os. we can have windows or mac os
    steps: #steps in the pipeline
      - name: step1 
        if: github.ref == 'refs/heads/feature-**'
        run: echo "hello world"
      - name: Step2
        
        if: github.ref == 'refs/heads/main'
        run: echo "Hello github actions" 
  