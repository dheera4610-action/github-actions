name: concurrency
on: [push] #triggers
concurrency:
 # this workflow should be triggered for any branch only once
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true # existing will be cancled and latest will be run
jobs: 
  hello: 
    runs-on: ubuntu-latest # linux os. we can have windows or mac os
    steps: #steps in the pipeline
      - name: step1 
        run: sleep 20